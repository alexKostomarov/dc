export const total = 8000;
export const firstBatch = 100;
export const batchSize = 500;
export const delay = 500;
export const perPage = 50;
export const pageRadius = 5; // how many pages to show on each side of the current page